apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: manyfold-data
  namespace: manyfold
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi

apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: calibre-headers
  namespace: media
spec:
  headers:
    customRequestHeaders:
      X-Forwarded-Proto: "https"
    sslProxyHeaders:
      X-Forwarded-Proto: "https"
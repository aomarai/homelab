apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: arrstalledhandler
  namespace: media
spec:
  replicas: 1
  selector:
    matchLabels:
      app: arrstalledhandler
  template:
    metadata:
      labels:
        app: arrstalledhandler
    spec:
      containers:
        - name: arrstalledhandler
          image: ghcr.io/linuxserver/arrstalledhandler:latest
          env:
            - name: PUID
              value: "99"       # match Unraid nobody user
            - name: PGID
              value: "100"      # match Unraid users group
            - name: TZ
              value: America/Chicago
          ports:
            - containerPort: 7878
          volumeMounts:
            - name: config
              mountPath: /config
            - name: media
              mountPath: /media
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: media-nfs
  volumeClaimTemplates:
    - metadata:
        name: config
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: longhorn
        resources:
          requests:
            storage: 5Gi
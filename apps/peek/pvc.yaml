apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: peek-data
  namespace: peek
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
